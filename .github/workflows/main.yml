name: Keep Render Alive

on:
  schedule:
    - cron: "*/14 * * * *"   # Runs every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render Health Endpoint
        run: |
          curl -f https://finance.tushargoel.in/health
